[pytest]
testpaths = tests
markers =
    slow: marks tests as slow (require model loading, >30s)
    models: marks tests that require GGUF model files
    inference: marks tests that run full inference
